<%page args="field, operation"/>

# field
# operation
{
  grok {
    dictionaryFiles : ["grok_dictionaries"]
    expressions : {
      ${field['name']}: """${operation['settings']['regexp']}"""
    }
  }
}